name: emotionalDairy Notification on Push

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Push detected on main branch!"
